# =============================================================================
# Telegram Weather Bot for Paragliders - Environment
# =============================================================================
# Copy this file to .env and fill in BOT_TOKEN.
# Other settings (API keys, timezone, polling, etc.) are stored in TOML in the DB.
# On first run, if DB config is empty, the app seeds from .env (you can temporarily
# add OPENWEATHER_API_KEY, VISUALCROSSING_API_KEY, etc. to .env for seeding, then remove).

# -----------------------------------------------------------------------------
# TELEGRAM BOT (required)
# -----------------------------------------------------------------------------
BOT_TOKEN=your_telegram_bot_token_here
